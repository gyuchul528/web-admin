<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>エラー - 管理画面</title>
    <!-- Bootstrap 5.3.0 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            height: 100vh;
            display: flex;
            align-items: center;
            background-color: #f5f5f5;
        }

        .error-container {
            max-width: 600px;
            width: 100%;
            padding: 15px;
            margin: auto;
            text-align: center;
        }

        .error-code {
            font-size: 6rem;
            font-weight: bold;
            color: #1696d7;
            margin-bottom: 1rem;
        }

        .error-message {
            font-size: 1.5rem;
            color: #666;
            margin-bottom: 2rem;
        }

        .error-details {
            color: #999;
            margin-bottom: 2rem;
        }

        .btn-back {
            padding: 0.8rem 2rem;
            font-size: 1.1rem;
        }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-code">
            <%= status || 500 %>
        </div>
        <div class="error-message">
            <%= message || 'エラーが発生しました' %>
        </div>
        <% if (error && error.stack && process.env.NODE_ENV === 'development') { %>
            <div class="error-details">
                <pre class="text-start"><%= error.stack %></pre>
            </div>
        <% } %>
        <button type="button" class="btn btn-primary btn-back" onclick="history.back()">
            前のページに戻る
        </button>
    </div>

    <!-- Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 