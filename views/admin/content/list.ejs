<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= category.name %> - コンテンツ一覧</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            padding: 8px;
            text-align: left;
            border: 1px solid #ddd;
        }
        th {
            background-color: #f5f5f5;
        }
        .operation-buttons {
            margin-bottom: 20px;
        }
        .operation-buttons button {
            padding: 5px 15px;
            margin-right: 10px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <h1><%= category.name %> - コンテンツ一覧</h1>
    
    <div class="operation-buttons">
        <button onclick="addContent()">新規追加</button>
    </div>

    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>タイトル</th>
                <th>内容</th>
                <th>状態</th>
                <th>作成日</th>
                <th>更新日</th>
                <th>操作</th>
            </tr>
        </thead>
        <tbody>
            <% if (contents && contents.length > 0) { %>
                <% contents.forEach(function(content) { %>
                    <tr>
                        <td><%= content.id %></td>
                        <td><%= content.title %></td>
                        <td><%= content.content ? content.content.substring(0, 50) + '...' : '' %></td>
                        <td><%= content.status_text %></td>
                        <td><%= new Date(content.created_at).toLocaleString() %></td>
                        <td><%= new Date(content.updated_at).toLocaleString() %></td>
                        <td>
                            <a href="/admin/content/edit/<%= content.id %>">編集</a>
                            <a href="javascript:void(0)" onclick="deleteContent('<%= content.id %>')">削除</a>
                        </td>
                    </tr>
                <% }); %>
            <% } else { %>
                <tr>
                    <td colspan="7" style="text-align: center;">コンテンツがありません</td>
                </tr>
            <% } %>
        </tbody>
    </table>

    <script>
        function addContent() {
            window.location.href = '/admin/content/add/<%= category.id %>';
        }

        function deleteContent(id) {
            if(confirm('本当に削除してもよろしいですか？')) {
                fetch(`/admin/content/delete/${id}`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    }
                })
                .then(response => response.json())
                .then(result => {
                    if(result.success) {
                        alert(result.message);
                        window.location.reload();
                    } else {
                        alert(result.message || '削除に失敗しました');
                    }
                })
                .catch(error => {
                    console.error('Error:', error);
                    alert('削除に失敗しました');
                });
            }
        }
    </script>
</body>
</html> 